export const GET_ALL_SUCCESS = (itemName: string) =>
    `Successfully got all ${itemName}s.`;

export const GET_SUCCESS = (itemName: string) =>
    `Successfully got ${itemName}.`;

export const CREATE_SUCCESS = (itemName: string) =>
    `Successfully created ${itemName.toLowerCase()}.`;

export const DELETE_SUCCESS = (itemName: string) =>
    `Successfully deleted ${itemName.toLowerCase()}.`;

export const UPDATE_SUCCESS = (itemName: string) =>
    `Successfully updated ${itemName.toLowerCase()}.`;
