export const CREATE_SUCCESS = (itemName: string) =>
    `Successfully created ${itemName.toLocaleLowerCase()}`;

export const DELETE_SUCCESS = (itemName: string) =>
    `Successfully deleted ${itemName.toLocaleLowerCase()}`;
